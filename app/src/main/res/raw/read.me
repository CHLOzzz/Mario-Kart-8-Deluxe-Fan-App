All files in the /res/raw file are data files.

Files ending in .csv contain all the data of the given entity class.
IE "drivers.csv" contains all the data for specifically all driver entities.
CSV files are formatted with, in order, 1 Integer(CSV entry #), 1 String (name) and 14 Integers
(stats) separated with commas. There should only be spaces in the name String)

Files ending in .webp are named appropriately, and contain displayable icons of each entity.